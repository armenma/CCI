'use strict';

module.exports = function(CryptoCurrencyIndexes) {

  /*CryptoCurrencyIndexes.remoteMethod('today', {

    http: {path: '/today', verb: 'get'},
    accepts: [
      { arg: 'filter', type: 'object', 'http': { source: 'query' } }
    ],
    returns: { type: 'object', root: true }
  });

  CryptoCurrencyIndexes.today = function(cb) {

    CryptoCurrencyIndexes.find({where:{value:{gt: 10}}}, function(err, result) {

      cb(null, result);
    });
  };*/


};
